const fs = require('fs');const path = require('path');const inquirer = require('inquirer');const envPath = path.resolve(__dirname, '../../../../../../../../../../../../../../../../../../../../../../../../../../../../../../.env.local');async function setupMemoryMonitoring() {  const answers = await inquirer.prompt([    {      type: 'confirm',      name: 'enable',      message: 'Enable memory monitoring?',      default: true,    },  ]);  if (answers.enable) {    let envContent = fs.existsSync(envPath) ? fs.readFileSync(envPath, 'utf-8') : '';    envContent += '\nENABLE_MEMORY_MONITORING=true';    fs.writeFileSync(envPath, envContent);    console.log('Memory monitoring enabled.');  }}setupMemoryMonitoring();