const fs = require('fs');const path = require('path');const inquirer = require('inquirer');const envPath = path.resolve(__dirname, '../../../../../../../../../../../../../../../../../../../../../../../../../../../../../../.env.local');async function setupAutoScaling() {  const answers = await inquirer.prompt([    {      type: 'confirm',      name: 'enable',      message: 'Enable auto-scaling?',      default: true,    },  ]);  if (answers.enable) {    let envContent = fs.existsSync(envPath) ? fs.readFileSync(envPath, 'utf-8') : '';    envContent += '\nENABLE_AUTO_SCALING=true';    fs.writeFileSync(envPath, envContent);    console.log('Auto-scaling enabled.');  }}setupAutoScaling();